@model StudentManagementSystem.Models.Student
@{
    ViewBag.Title = "Add Student";
    ViewBag.ActivePage = "Students";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="page-header">
    <div>
        <div class="page-title">Add New Student</div>
        <div class="page-subtitle">// Fill in student details below</div>
    </div>
    <a href="@Url.Action("Students","Admin")" class="btn-edu btn-edu-secondary">
        <i class="bi bi-arrow-left"></i> Back to Students
    </a>
</div>

<div style="max-width:760px">
    @using (Html.BeginForm("CreateStudent", "Admin", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        @Html.AntiForgeryToken()

        <div class="edu-card mb-3">
            <div class="edu-card-header">
                <span class="edu-card-title"><i class="bi bi-person-vcard me-2 text-teal"></i>Personal Information</span>
            </div>
            <div class="edu-card-body">
                <div class="form-grid">
                    <div class="field-group">
                        <label class="field-label">First Name *</label>
                        @Html.TextBoxFor(m => m.FirstName, new { @class = "edu-input", placeholder = "e.g. Muhammad" })
                        @Html.ValidationMessageFor(m => m.FirstName, "", new { @class = "field-error" })
                    </div>
                    <div class="field-group">
                        <label class="field-label">Last Name *</label>
                        @Html.TextBoxFor(m => m.LastName, new { @class = "edu-input", placeholder = "e.g. Rehan" })
                        @Html.ValidationMessageFor(m => m.LastName, "", new { @class = "field-error" })
                    </div>
                    <div class="field-group">
                        <label class="field-label">Email Address *</label>
                        @Html.TextBoxFor(m => m.Email, new { @class = "edu-input", placeholder = "student@email.com" })
                        @Html.ValidationMessageFor(m => m.Email, "", new { @class = "field-error" })
                    </div>
                    <div class="field-group">
                        <label class="field-label">Phone Number *</label>
                        @Html.TextBoxFor(m => m.PhoneNumber, new { @class = "edu-input", placeholder = "+92 300 0000000" })
                        @Html.ValidationMessageFor(m => m.PhoneNumber, "", new { @class = "field-error" })
                    </div>
                    <div class="field-group">
                        <label class="field-label">Date of Birth *</label>
                        @Html.TextBoxFor(m => m.DateOfBirth, "{0:yyyy-MM-dd}", new { @class = "edu-input", type = "date" })
                        @Html.ValidationMessageFor(m => m.DateOfBirth, "", new { @class = "field-error" })
                    </div>
                    <div class="field-group">
                        <label class="field-label">Profile Photo</label>
                        <input type="file" name="PhotoFile" class="edu-input" accept="image/*" />
                        <div class="field-hint">// JPG, PNG — max 2MB</div>
                    </div>
                    <div class="field-group form-grid-full">
                        <label class="field-label">Address *</label>
                        @Html.TextAreaFor(m => m.Address, new { @class = "edu-textarea", placeholder = "Street, City, Country" })
                        @Html.ValidationMessageFor(m => m.Address, "", new { @class = "field-error" })
                    </div>
                </div>
            </div>
        </div>

        <div style="display:flex;gap:10px">
            <button type="submit" class="btn-edu btn-edu-primary">
                <i class="bi bi-check-lg"></i> Save Student
            </button>
            <a href="@Url.Action("Students","Admin")" class="btn-edu btn-edu-secondary">
                <i class="bi bi-x-lg"></i> Cancel
            </a>
        </div>
    }
</div>
